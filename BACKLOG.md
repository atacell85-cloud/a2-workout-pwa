# Backlog

## Offline reconnect cloud-sync E2E + persistent dirty-state verification

Verify, on the public deployment and a clean browser context, that offline local mutations retain account-scoped dirty metadata across reload, automatically push after reconnect, clear dirty state only after a successful response, and restore from cloud without local-cache false positives.
